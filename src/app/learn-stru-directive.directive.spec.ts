import { LearnStruDirectiveDirective } from './learn-stru-directive.directive';

describe('LearnStruDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LearnStruDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
